#
# Configuration options for building are stored in the Makefile.in file.
# This file is generated by running the ./configure script.
#
# 

CC = gcc

MFLAGS =
KERNEL=`uname -r`
IFLAGS = -I/lib/modules/$(KERNEL)/build/include
CFLAGS = -O3 $(IFLAGS)
VERSION = 0.02

#
# The Objects...
#
OBJS	= chos_lkm.o 

all:  chos.o

chos.o:  $(OBJS)
	ld -o chos.o -r $(OBJS)
#	/usr/bin/strip $@

clean: 
	rm -f $(OBJS) chos chos.o
#	rm -f Makefile.in


test:
	/sbin/insmod chos.o
